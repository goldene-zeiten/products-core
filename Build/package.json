{
  "name": "products-core-build",
  "description": "Build toolchain (TypeScript) for the goldene-zeiten/products-core TYPO3 extension.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build:js",
    "build:js": "node build/build-js.mjs"
  },
  "devDependencies": {
    "lit": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
